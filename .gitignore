# ─────────────────────────────
# Rust / Cargo 相关
# ─────────────────────────────
/target/
**/target/

# 临时构建目录（Windows 链接器锁规避，测试隔离输出）
/target_temp/
**/target_temp/

# Rust 增量编译 / 临时构建文件
**/*.rs.bk

# build 脚本可能的输出（保留 genesis_hash.rs）
/build/
**/*.generated.rs
!src/binary_verify/genesis_hash.rs

# Rust 工具输出
target/doc/
Clippy.toml
.rustfmt.toml

# ─────────────────────────────
# 项目特定目录 / 存储 / 运行时产物
# ─────────────────────────────

# 原始文件目录 (raw files) — 避免把大文件、临时未加密文件提交
/storage/raw_files/
/raw_shares/

# 数据库 / 存储文件
/storage/**/*.db*
*.db-shm
*.db-wal

# mmap 块目录和可能的二进制 / pad 数据
/mmap_blocks/
*.mmap
*.pad
*.bin

# ─────────────────────────────
# 测试覆盖率 / 代码分析工具
# ─────────────────────────────

# 覆盖率文件
/coverage/
*.profraw
*.profdata
lcov.info
cargo-test-*.json
miri/

# ─────────────────────────────
# 子模块 / 第三方库构建产物
# ─────────────────────────────

# 如果你的 third_party 目录里有 Rust / C /其他语言子模块
/third_party/**/target/
/third_party/**/debug/
/third_party/**/build/
/vendor/

# ─────────────────────────────
# 锁 / 并发 / platform 特殊文件
# ─────────────────────────────

# 如果你有 lock 模式（memguard、锁文件等）生成
*.lockguard
*.memlock

# ─────────────────────────────
# 操作系统 / 编辑器 / IDE
# ─────────────────────────────

.DS_Store
Thumbs.db
ehthumbs.db

.vscode/
.idea/
*.swp
*.swo
*.iml
*.code-workspace

# ─────────────────────────────
# 日志 / 临时文件
# ─────────────────────────────

*.log
logs/
temp/
tmp/
*.tmp

# ─────────────────────────────
# C 头文件 / 绑定
# ─────────────────────────────

# 生成的头文件和 FFI 配置
/include/*.h
cbindgen.toml
/target/bindings/

# ─────────────────────────────
# 环境变量 / 配置
# ─────────────────────────────

.env
.env.local
.env.*.local

# ─────────────────────────────
# CI / GitHub Actions 输出
# ─────────────────────────────

# CI 构建产物和缓存
.github/workflows/**/target/
.github/workflows/**/artifact/
.github/workflows/*.cache

# ─────────────────────────────
# 文档 / 示例构建
# ─────────────────────────────

docs/target/
examples/target/
